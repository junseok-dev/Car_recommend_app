# Car_recommend_app

# 🚗 자동차 추천 RAG 시스템 - 배포 가이드

## 로컬 실행 (Local)

### 1. 필수 패키지 설치

```bash
pip install -r requirements.txt
```

### 2. .env 파일 생성

프로젝트 루트에 `.env` 파일을 생성하고 OpenAI API 키를 추가:

```
OPENAI_API_KEY=sk-your-api-key-here
```

### 3. Streamlit 실행

```bash
streamlit run app.py
```

---

## Streamlit Cloud 배포

### 1. GitHub에 업로드

- 리포지토리에 다음 파일 포함:
  - `app.py`
  - `requirements.txt`
  - `data/Cars_Datasets_2025.csv`

### 2. Streamlit Cloud에서 배포

1. [Streamlit Cloud](https://streamlit.io/cloud) 방문
2. 계정 생성 및 로그인
3. "New app" → GitHub 리포지토리 선택
4. Deploy 클릭

### 3. Secrets 설정

배포 후 다음 단계를 따르세요:

1. 앱 정보 페이지 → **Settings** (톱니바퀴 아이콘)
2. **Secrets** 탭 선택
3. 다음 내용 입력:

```
OPENAI_API_KEY = "sk-your-api-key-here"
```

### 4. 앱 재실행

설정 저장 후 자동으로 앱이 재실행됩니다.

---

## 주의사항

⚠️ **API 키 보안**

- `.env` 파일을 `.gitignore`에 추가 (절대 GitHub에 커밋하지 말 것)
- Streamlit Cloud Secrets에 API 키 저장 (암호화됨)

---

## 문제 해결

### "API 키가 필요합니다" 메시지가 나타나는 경우

1. 로컬 실행: `.env` 파일에 `OPENAI_API_KEY` 추가 확인
2. 클라우드 배포: Streamlit Cloud의 **Secrets**에 `OPENAI_API_KEY` 추가 확인

### CSV 파일을 찾을 수 없는 경우

- `data/Cars_Datasets_2025.csv` 파일이 프로젝트에 포함되어 있는지 확인
- 배포 전 GitHub에 업로드 확인

---

## 환경 변수 로드 순서

앱은 다음 순서로 API 키를 찾습니다:

1. **Streamlit Secrets** (배포 환경)
2. **.env 파일** (로컬 환경)
3. **UI 입력** (테스트용)
